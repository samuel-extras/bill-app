import React from 'react';
import { View, StyleSheet } from 'react-native';

const confirmation = (props) => {
  return (
      <View style={styles.container}>cd </View>
  );
}
const styles = StyleSheet.create({
  container: {},
});

export default confirmation;